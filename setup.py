#!/usr/bin/env python3

from setuptools import setup, find_packages

setup(
    name='ypack',
    version='0.0.1',
    packages=find_packages(),
    url='',
    license='MIT',
    author='yk',
    author_email='',
    description='',
    install_requires=['tqdm', 'numpy']
)
